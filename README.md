# Velocity Logic

This repository contains the source code for the Velocity Logic application.

## Structure

- **frontend/**: The React/Vite web application.
- **backend/**: The Python/Flask backend and agent logic.

## Deployment

### Frontend
Deploy the `frontend` directory to Vercel.

### Backend
The `backend` is a Python application. Ensure environment variables are set up correctly.
